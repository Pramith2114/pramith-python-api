version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies
      - pip install -r requirements.txt

  build:
    commands:
      - echo Build started
      - echo No build needed for simple Flask app

artifacts:
  files:
    - '**/*'